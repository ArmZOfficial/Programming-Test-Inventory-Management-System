/**
 * ไอคอนทั้งระบบ — ใช้ SVG จาก lucide-react (bundle มากับแอป ไม่ต้องโหลดจาก CDN)
 *
 * รวมไว้ที่ไฟล์เดียวเพื่อให้เปลี่ยนชุดไอคอนทีหลังได้โดยแก้ที่นี่ที่เดียว
 * ทุกไอคอนเป็น stroke SVG สีตาม currentColor จึงเข้ากับธีมสว่าง/มืดอัตโนมัติ
 */
export {
  AlertTriangle as IconWarning,
  ArrowDownToLine as IconIn,
  ArrowLeft as IconArrowLeft,
  ArrowRight as IconArrowRight,
  ArrowUpFromLine as IconOut,
  Ban as IconOutOfStock,
  Boxes as IconBoxes,
  CheckCircle2 as IconSuccess,
  ChevronLeft as IconPrev,
  ChevronRight as IconNext,
  CircleAlert as IconError,
  Coins as IconMoney,
  Compass as IconCompass,
  History as IconHistory,
  Info as IconInfo,
  Inbox as IconEmpty,
  Lightbulb as IconTip,
  Menu as IconMenu,
  Moon as IconMoon,
  Package as IconPackage,
  PackageSearch as IconPackageSearch,
  PartyPopper as IconCelebrate,
  Plug as IconPlug,
  Plus as IconPlus,
  RefreshCw as IconRefresh,
  Search as IconSearch,
  Sun as IconSun,
  Tag as IconTag,
  Bell as IconBell,
  X as IconClose,
} from 'lucide-react';
